[workspace]
members = ["server", "port-forward"]
resolver = "2"

[workspace.package]
version = "0.1.0"
authors = ["you"]
edition = "2021"

[workspace.profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
